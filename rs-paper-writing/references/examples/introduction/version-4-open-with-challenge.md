# Version 4: Open With Challenge

Use this when the task is familiar and the limitation can be stated immediately.

```text
LLM-based recommendation has shown promise by converting user histories into prompts and generating recommended items directly.
However, generated outputs may be invalid or difficult to ground to the item catalog, which makes ranking improvements unreliable unless output validity is explicitly handled.
```

Alternative:

```text
Sequential recommenders have achieved strong performance by modeling temporal user behavior.
However, they often treat all historical interactions as equally reliable signals, making them vulnerable to noisy short-term actions and interest drift.
```
