# Pipeline Version 1: One Contribution With Multiple Advantages

Use when one core contribution solves several problems.

```text
To address this problem, we propose [Name], a [model] that represents user interest with [core representation].
The key idea is to use [representation/objective] as a shared signal for both relevance modeling and robustness.
Specifically, [Name] first encodes the user's interaction history into [representation], then uses this representation to score candidate items.
This design improves recommendation accuracy because it captures [advantage 1], and it improves robustness because it reduces [advantage 2].
```
