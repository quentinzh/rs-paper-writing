# Pipeline Version 2: Two Contributions

Use when the method has two necessary technical pieces.

```text
To address this problem, we propose [Name], a framework for LLM-based recommendation.
First, [Name] introduces [module 1] to ensure that generated outputs can be mapped to valid catalog items.
However, valid generation alone does not guarantee high-quality ranking.
Therefore, [Name] further designs [module 2] to align the generated list with recommendation metrics.
Together, these modules improve both output validity and ranking quality.
```
