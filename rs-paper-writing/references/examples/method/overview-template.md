# Method Overview Template

```text
We propose [Name], a [model/method/framework/objective/algorithm] for [task].
[Name] addresses [problem] by [core idea].
As shown in Figure [X], the framework contains three components: [component 1], [component 2], and [component 3].
Section [X.1] describes [component 1], Section [X.2] introduces [component 2], and Section [X.3] presents the training objective and inference procedure.
```

LLM-based variant:

```text
Given a user history, [Name] first constructs a prompt, then generates [output type], and finally maps the output to valid catalog items through [grounding/constrained decoding].
The model is optimized with [loss/objective], which encourages [recommendation goal].
```
